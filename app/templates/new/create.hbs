<form {{action create on="submit"}} role="form">
	<div class="input-group">
		{{input value=value placeholder=placeholder}}
		<span class="input-group-btn">
			<button type="submit" class="btn">
				{{sniffy-icon type="plus"}}
			</button>
		</span>
	</div>
</form>